import React from "react"
import s from "./accordion-specs.module.scss"

type Props = {}

const AccordionSpecs = (props: Props) => {
  return <div className={s.accordionSpecs}>AccordionSpecs</div>
}

export default AccordionSpecs
